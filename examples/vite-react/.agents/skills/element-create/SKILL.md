---
name: element-create
description: >
  이 프로젝트에서 UI 컴포넌트를 새로 만들거나 수정·확장하려는 모든 요청에 발동한다. 기존 컴포넌트를
  먼저 추천(재사용)하고, 없을 때만 새로 만든 뒤 레지스트리에 등록하고 갤러리 딥링크를 낸다.
  트리거 — "/element-create", "컴포넌트 만들어줘", "버튼/카드/모달/… 필요해", "이런 UI 만들자",
  "이 컴포넌트 수정해줘", "이 컴포넌트에 ~ 추가해줘". 다음에는 발동하지 않는다 — 순수 로직·데이터
  작업, 스타일과 무관한 리팩터링, 이미 만든 컴포넌트를 단순히 import 해 쓰기만 하는 경우,
  페이지 조립(page-create), instant-elements 라이브러리 자체의 수정.
---

# element-create — 포인터 (정본은 GUIDE)

> ⚠️ **이 파일은 트리거 등록용 얇은 포인터다. 실제 절차와 하드룰은 여기 없다.**
>
> 이 스킬이 발동하면 **작업을 시작하기 전에 반드시** 정본을 새로 열어 그대로 따른다:
>
> ```bash
> npx instant-elements guide element-create
> ```
>
> - 정본은 설치된 패키지 안에 있어 **`npm update instant-elements` 만으로 최신 지침을 받는다**
>   (스킬 재설치 불필요 — 이 포인터는 거의 바뀌지 않는다).
> - 프로젝트가 `.instant/skills/element-create/GUIDE.md` 를 두면 그쪽이 정본이 된다(팀 규칙 추가).
> - **이 파일의 요약에 의존하지 말고** GUIDE 를 열어 그 절차대로 실행한다.
> - GUIDE 를 못 읽으면 임의로 진행하지 말고 사용자에게 알리고 멈춘다.

한 줄 요약(정본 대체 아님): 재사용을 먼저 찾고, 없을 때만 만든다. 색은 `st-*` 토큰만, 크기·여백은
스케일만. 고쳤으면 반드시 기록한다. — **자세한 절차는 `npx instant-elements guide element-create`.**
