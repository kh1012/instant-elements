/**
 * 프롬프트에 싣는 CLI 호출 접두사.
 *
 * **`ie` 는 실행 파일 이름이고 `instant-elements` 는 패키지 이름이다.** `npx` 는 패키지를
 * 받아오므로 `npx ie` 는 npm 에 따로 존재하는 남의 `ie` 패키지를 내려받는다 — 로컬에서만
 * `node_modules/.bin/ie` 가 먼저 잡혀 우연히 동작하고, 깨끗한 환경에서는 엉뚱한 것이 깔린다.
 *
 * 한 곳에 모아 둔 이유는 그 사고가 이미 한 번 났기 때문이다. 13개 파일을 일괄 치환했는데
 * 프롬프트 빌더 일부가 빠져 `ie` 와 `npx instant-elements` 가 **섞인 채로** 배포됐다.
 * 문자열을 여러 곳에서 손으로 쓰는 한 다음에도 같은 방식으로 어긋난다.
 *
 * 사람이 읽는 산문에서 명령을 **지칭**할 때는 짧은 `ie` 를 그대로 써도 된다(그게 실제 이름이다).
 * 여기 있는 것은 **복사해서 실행될** 줄에만 쓴다.
 */
export const CLI = "npx instant-elements";
